import csv

#Iterate through all IP's in the student sumbissions list.
#For each IP use the IP to netID appropiate mapping to find which students work it is
#Take their JSON submission, and iterate through each question.
#At each question, take the desired information from the structure and save them within the CSV